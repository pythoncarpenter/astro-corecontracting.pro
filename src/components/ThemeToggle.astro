---
// No frontmatter needed for this component
---
<button id="theme-toggle" class="theme-toggle-btn p-2 rounded focus:outline-none" aria-label="Toggle Light/Dark">
  <span class="theme-icon flex items-center justify-center"></span>
</button>

<script is:inline>
  // Define SVG icons for visual feedback
  const sunIcon = `
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none"
         stroke="currentColor" stroke-width="2" stroke-linecap="round"
         stroke-linejoin="round" class="w-6 h-6 text-yellow-300">
      <circle cx="12" cy="12" r="5" />
      <path d="M12 1v2M12 21v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M1 12h2M21 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42" />
    </svg>
  `;

  const moonIcon = `
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" fill="none"
         stroke="currentColor" stroke-width="2" stroke-linecap="round"
         stroke-linejoin="round" class="w-6 h-6 text-blue-700">
      <path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z" />
    </svg>
  `;

  // Function to update the toggle button icons across the page
  function updateAllToggleIcons(isDark) {
    document.querySelectorAll('.theme-icon').forEach((iconEl) => {
      iconEl.innerHTML = isDark ? sunIcon : moonIcon;
    });
  }

  // Determine the initial theme:
  // - Use stored value if available
  // - Otherwise, use system preference (via matchMedia)
  const storedTheme = localStorage.getItem('colorScheme');
  const prefersDark = window.matchMedia('(prefers-color-scheme: dark)').matches;
  const isDarkMode = storedTheme ? storedTheme === 'dark' : prefersDark;

  if (isDarkMode) {
    document.documentElement.classList.add('dark');
  } else {
    document.documentElement.classList.remove('dark');
  }
  localStorage.setItem('colorScheme', isDarkMode ? 'dark' : 'light');
  updateAllToggleIcons(isDarkMode);

  // Function to toggle the theme on user action
  function toggleTheme() {
    const currentlyDark = document.documentElement.classList.contains('dark');
    const newThemeIsDark = !currentlyDark;
    if (newThemeIsDark) {
      document.documentElement.classList.add('dark');
      localStorage.setItem('colorScheme', 'dark');
    } else {
      document.documentElement.classList.remove('dark');
      localStorage.setItem('colorScheme', 'light');
    }
    updateAllToggleIcons(newThemeIsDark);
  }

  // Attach event listener to the toggle button
  document.querySelectorAll('.theme-toggle-btn').forEach((btn) => {
    btn.addEventListener('click', toggleTheme);
  });
</script>