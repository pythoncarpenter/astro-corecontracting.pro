---
import Layout from '../layouts/Layout.astro';
import { useState } from 'solid-js';

// State to manage form visibility and success message
const [formSubmitted, setFormSubmitted] = useState(false);

async function handleFormSubmit(event) {
  event.preventDefault();

  const formData = new FormData(event.target);

  // Send data to API
  const response = await fetch('/api/contact', {
    method: 'POST',
    body: formData,
  });

  // If successful, show success message
  if (response.ok) {
    setFormSubmitted(true);
  } else {
    alert('Failed to send the message. Please try again.');
  }
}
---

<Layout title="Contact Us - Core Contracting">
  <div class="container mx-auto px-4 py-12">
    <h1 class="text-4xl font-display text-center mb-4">Contact Us</h1>
    <p class="text-center text-lg mb-12 max-w-2xl mx-auto">
      Get in touch for a free quote or to discuss your project
    </p>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-12">
      <!-- Form and Contact Details -->
      {formSubmitted ? (
        <!-- Success Message -->
        <div class="bg-gray-100 rounded-full p-10 shadow-lg text-center mx-auto transition-all duration-500">
          <div class="text-green-500 text-4xl mb-2">✔</div>
          <h2 class="text-2xl font-bold mb-2">Message Sent!</h2>
          <p class="text-lg text-gray-600">Thank you for reaching out. We'll get back to you soon.</p>
        </div>
      ) : (
        <>
          <!-- Left Section: Contact Form -->
          <div class="bg-white dark:bg-primary-800 p-8 rounded-lg shadow-lg">
            <h2 class="text-2xl font-bold mb-6">Send Us a Message</h2>
            <form onSubmit={handleFormSubmit} class="space-y-4">
              <div>
                <label class="block text-sm font-medium mb-1" for="name">Name</label>
                <input
                  type="text"
                  id="name"
                  name="name"
                  required
                  class="w-full px-4 py-2 rounded-lg border border-primary-300 dark:border-primary-600 focus:ring-2 focus:ring-wood-light dark:bg-primary-700"
                />
              </div>
              <div>
                <label class="block text-sm font-medium mb-1" for="email">Email</label>
                <input
                  type="email"
                  id="email"
                  name="email"
                  required
                  class="w-full px-4 py-2 rounded-lg border border-primary-300 dark:border-primary-600 focus:ring-2 focus:ring-wood-light dark:bg-primary-700"
                />
              </div>
              <div>
                <label class="block text-sm font-medium mb-1" for="message">Message</label>
                <textarea
                  id="message"
                  name="message"
                  rows="4"
                  required
                  class="w-full px-4 py-2 rounded-lg border border-primary-300 dark:border-primary-600 focus:ring-2 focus:ring-wood-light dark:bg-primary-700"
                ></textarea>
              </div>
              <button
                type="submit"
                class="w-full bg-wood-dark hover:bg-wood text-white px-6 py-3 rounded-lg transition-colors"
              >
                Send Message
              </button>
            </form>
          </div>

          <!-- Right Section: Contact Information -->
          <div class="space-y-8">
            <div>
              <h2 class="text-2xl font-bold mb-4">Contact Information</h2>
              <div class="space-y-4">
                <div class="flex items-center">
                  <svg class="h-6 w-6 text-wood-dark dark:text-wood-light mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 5a2 2 0 012-2h3.28a1 1 0 01.948.684l1.498 4.493a1 1 0 01-.502 1.21l-2.257 1.13a11.042 11.042 0 005.516 5.516l1.13-2.257a1 1 0 011.21-.502l4.493 1.498a1 1 0 01.684.949V19a2 2 0 01-2 2h-1C9.716 21 3 14.284 3 6V5z"/>
                  </svg>
                  <a href="tel:+17054987194" class="hover:text-wood-dark dark:hover:text-wood-light">
                    (705) 498-7194
                  </a>
                </div>
                <div class="flex items-center">
                  <svg class="h-6 w-6 text-wood-dark dark:text-wood-light mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 8l7.89 5.26a2 2 0 002.22 0L21 8M5 19h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v10a2 2 0 002 2z"/>
                  </svg>
                  <a href="mailto:mitch.mcquoid@gmail.com" class="hover:text-wood-dark dark:hover:text-wood-light">
                    mitch.mcquoid@gmail.com
                  </a>
                </div>
                <div class="flex items-center">
                  <svg class="h-6 w-6 text-wood-dark dark:text-wood-light mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
                  </svg>
                  <span>North Bay, Ontario</span>
                </div>
              </div>
            </div>
          </div>
        </>
      )}
    </div>
  </div>
</Layout>